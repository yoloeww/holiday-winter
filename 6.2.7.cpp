#include <iostream>using namespace std;int main() {    int n;    cin >> n;    int num = 0;    for (int i = 1; i <= n; i++){        if (i % 7 == 0)        num++;        else {        string s = to_string(i);        for(int j = 0; j < s.size(); j++) {            if(s[j] == '7'){            num++;            break;            }        }        }    }    cout << num << endl;    return 0;}// 64 位输出请用 printf("%lld")
